{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-almanac-day",
  "type": "registry:ui",
  "title": "RoxyAlmanacDay",
  "description": "Chinese almanac (Tong Shu): the day officer, what the day favours and avoids, the animal it clashes with, its three pillars and lunar mansion, for one day, a whole month, or a search for a date to act on",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.37.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-almanac-day.tsx",
      "content": "'use client';\n\n/**\n * <RoxyAlmanacDay data={...} />: Chinese almanac (Tong Shu): the day officer, what the day favours and avoids, the animal it clashes with, its three pillars and lunar mansion, for one day, a whole month, or a search for a date to act on.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/almanac-day/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyAlmanacDay as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyAlmanacDayProps = ComponentProps<typeof Element>;\n\nexport function RoxyAlmanacDay(props: RoxyAlmanacDayProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyAlmanacDay;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/almanac-day.tsx"
    }
  ]
}