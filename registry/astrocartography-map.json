{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-astrocartography-map",
  "type": "registry:ui",
  "title": "RoxyAstrocartographyMap",
  "description": "Astrocartography world map plotting each body Midheaven, IC, Ascendant, and Descendant relocation lines",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.11.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-astrocartography-map.tsx",
      "content": "'use client';\n\n/**\n * <RoxyAstrocartographyMap data={...} />: Astrocartography world map plotting each body Midheaven, IC, Ascendant, and Descendant relocation lines.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/astrocartography-map/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyAstrocartographyMap as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyAstrocartographyMapProps = ComponentProps<typeof Element>;\n\nexport function RoxyAstrocartographyMap(props: RoxyAstrocartographyMapProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyAstrocartographyMap;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/astrocartography-map.tsx"
    }
  ]
}