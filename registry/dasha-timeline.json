{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-dasha-timeline",
  "type": "registry:ui",
  "title": "RoxyDashaTimeline",
  "description": "Vimshottari dasha timeline, mahadasha through prana, with the active period highlighted",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.34.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-dasha-timeline.tsx",
      "content": "'use client';\n\n/**\n * <RoxyDashaTimeline data={...} />: Vimshottari dasha timeline, mahadasha through prana, with the active period highlighted.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/dasha-timeline/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.vedicAstrology.getMajorDashas({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyDashaTimeline as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDashaTimelineProps = ComponentProps<typeof Element>;\n\nexport function RoxyDashaTimeline(props: RoxyDashaTimelineProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyDashaTimeline;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/dasha-timeline.tsx"
    }
  ]
}