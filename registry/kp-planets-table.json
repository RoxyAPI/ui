{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-kp-planets-table",
  "type": "registry:ui",
  "title": "RoxyKpPlanetsTable",
  "description": "KP planets table with sub-lord and sub-sub-lord columns",
  "dependencies": [
    "@roxyapi/ui-react",
    "@roxyapi/sdk"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@main/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-kp-planets-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyKpPlanetsTable data={...} /> — KP planets table with sub-lord and sub-sub-lord columns.\n *\n * You own this file. Customize freely. The underlying Lit element loads\n * from jsDelivr at runtime via @roxyapi/ui-react, so updates ship without\n * a re-install.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/kp-planets-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.vedicAstrology.getKpPlanets({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyKpPlanetsTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyKpPlanetsTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyKpPlanetsTable(props: RoxyKpPlanetsTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyKpPlanetsTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/kp-planets-table.tsx"
    }
  ]
}