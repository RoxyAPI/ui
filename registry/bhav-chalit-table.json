{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-bhav-chalit-table",
  "type": "registry:ui",
  "title": "RoxyBhavChalitTable",
  "description": "Which grahas change house once the bhavas are cut at their real cusps instead of at sign boundaries",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-bhav-chalit-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyBhavChalitTable data={...} />: Which grahas change house once the bhavas are cut at their real cusps instead of at sign boundaries.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/bhav-chalit-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyBhavChalitTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyBhavChalitTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyBhavChalitTable(props: RoxyBhavChalitTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyBhavChalitTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/bhav-chalit-table.tsx"
    }
  ]
}