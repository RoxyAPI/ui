{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-shadbala-table",
  "type": "registry:ui",
  "title": "RoxyShadbalaTable",
  "description": "Six-fold planetary strength with adequacy badge per planet",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.2.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-shadbala-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyShadbalaTable data={...} />: Six-fold planetary strength with adequacy badge per planet.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/shadbala-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyShadbalaTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyShadbalaTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyShadbalaTable(props: RoxyShadbalaTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyShadbalaTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/shadbala-table.tsx"
    }
  ]
}