{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-forecast-timeline",
  "type": "registry:ui",
  "title": "RoxyForecastTimeline",
  "description": "Cross-domain forecast event strip colored by domain and weighted by significance",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.17.1/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-forecast-timeline.tsx",
      "content": "'use client';\n\n/**\n * <RoxyForecastTimeline data={...} />: Cross-domain forecast event strip colored by domain and weighted by significance.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/forecast-timeline/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyForecastTimeline as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyForecastTimelineProps = ComponentProps<typeof Element>;\n\nexport function RoxyForecastTimeline(props: RoxyForecastTimelineProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyForecastTimeline;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/forecast-timeline.tsx"
    }
  ]
}