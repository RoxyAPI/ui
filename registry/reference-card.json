{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-reference-card",
  "type": "registry:ui",
  "title": "RoxyReferenceCard",
  "description": "Glossary entry for any reference lookup: zodiac sign, planet meaning, rashi, I Ching trigram, Human Design gate or center, numerology number or compound number",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.5/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-reference-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyReferenceCard data={...} />: Glossary entry for any reference lookup: zodiac sign, planet meaning, rashi, I Ching trigram, Human Design gate or center, numerology number or compound number.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/reference-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyReferenceCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyReferenceCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyReferenceCard(props: RoxyReferenceCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyReferenceCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/reference-card.tsx"
    }
  ]
}