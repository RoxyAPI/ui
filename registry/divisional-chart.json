{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-divisional-chart",
  "type": "registry:ui",
  "title": "RoxyDivisionalChart",
  "description": "D2 to D60 varga chart wheel with Vargottama markers",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.22.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-divisional-chart.tsx",
      "content": "'use client';\n\n/**\n * <RoxyDivisionalChart data={...} />: D2 to D60 varga chart wheel with Vargottama markers.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/divisional-chart/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.vedicAstrology.generateDivisionalChart({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyDivisionalChart as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDivisionalChartProps = ComponentProps<typeof Element>;\n\nexport function RoxyDivisionalChart(props: RoxyDivisionalChartProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyDivisionalChart;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/divisional-chart.tsx"
    }
  ]
}