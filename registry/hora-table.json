{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-hora-table",
  "type": "registry:ui",
  "title": "RoxyHoraTable",
  "description": "Vedic Hora (planetary hours) table: 12 day and 12 night horas by ruling planet for electional timing",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.29.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-hora-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyHoraTable data={...} />: Vedic Hora (planetary hours) table: 12 day and 12 night horas by ruling planet for electional timing.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/hora-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyHoraTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyHoraTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyHoraTable(props: RoxyHoraTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyHoraTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/hora-table.tsx"
    }
  ]
}