{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-tarot-spread",
  "type": "registry:ui",
  "title": "RoxyTarotSpread",
  "description": "Tarot spread renderer for three-card, Celtic Cross, love, or yes/no",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.18.4/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-tarot-spread.tsx",
      "content": "'use client';\n\n/**\n * <RoxyTarotSpread data={...} />: Tarot spread renderer for three-card, Celtic Cross, love, or yes/no.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/tarot-spread/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.tarot.castThreeCard({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyTarotSpread as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyTarotSpreadProps = ComponentProps<typeof Element>;\n\nexport function RoxyTarotSpread(props: RoxyTarotSpreadProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyTarotSpread;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/tarot-spread.tsx"
    }
  ]
}