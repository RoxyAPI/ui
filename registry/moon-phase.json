{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-moon-phase",
  "type": "registry:ui",
  "title": "RoxyMoonPhase",
  "description": "Moon phase card and calendar",
  "dependencies": [
    "@roxyapi/ui-react",
    "@roxyapi/sdk"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@main/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-moon-phase.tsx",
      "content": "'use client';\n\n/**\n * <RoxyMoonPhase data={...} /> — Moon phase card and calendar.\n *\n * You own this file. Customize freely. The underlying Lit element loads\n * from jsDelivr at runtime via @roxyapi/ui-react, so updates ship without\n * a re-install.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/moon-phase/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.astrology.getCurrentMoonPhase();\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyMoonPhase as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyMoonPhaseProps = ComponentProps<typeof Element>;\n\nexport function RoxyMoonPhase(props: RoxyMoonPhaseProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyMoonPhase;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/moon-phase.tsx"
    }
  ]
}