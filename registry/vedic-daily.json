{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-vedic-daily",
  "type": "registry:ui",
  "title": "RoxyVedicDaily",
  "description": "One native, one day: the verdict band, every graha state behind it, the Moon windows and the finance area",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.31.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-vedic-daily.tsx",
      "content": "'use client';\n\n/**\n * <RoxyVedicDaily data={...} />: One native, one day: the verdict band, every graha state behind it, the Moon windows and the finance area.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/vedic-daily/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyVedicDaily as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyVedicDailyProps = ComponentProps<typeof Element>;\n\nexport function RoxyVedicDaily(props: RoxyVedicDailyProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyVedicDaily;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/vedic-daily.tsx"
    }
  ]
}