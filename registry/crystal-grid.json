{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-crystal-grid",
  "type": "registry:ui",
  "title": "RoxyCrystalGrid",
  "description": "Responsive crystal gallery with photo, name, and colour swatches from any crystals list response",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.1/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-crystal-grid.tsx",
      "content": "'use client';\n\n/**\n * <RoxyCrystalGrid data={...} />: Responsive crystal gallery with photo, name, and colour swatches from any crystals list response.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/crystal-grid/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyCrystalGrid as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyCrystalGridProps = ComponentProps<typeof Element>;\n\nexport function RoxyCrystalGrid(props: RoxyCrystalGridProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyCrystalGrid;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/crystal-grid.tsx"
    }
  ]
}