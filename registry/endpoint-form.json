{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-endpoint-form",
  "type": "registry:ui",
  "title": "RoxyEndpointForm",
  "description": "Schema-driven form that emits roxy-submit with a validated payload",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.12.3/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-endpoint-form.tsx",
      "content": "'use client';\n\n/**\n * <RoxyEndpointForm data={...} />: Schema-driven form that emits roxy-submit with a validated payload.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Wiring:\n *\n * ```tsx\n * // <RoxyEndpointForm> introspects the OpenAPI spec at runtime and emits\n * // roxy-submit with a validated payload. Listen for the event and call\n * // any endpoint server-side:\n * <RoxyEndpointForm\n *   endpoint=\"/astrology/natal-chart\"\n *   onRoxySubmit={async (e) => {\n *     const res = await fetch('/api/natal-chart', {\n *       method: 'POST',\n *       body: JSON.stringify(e.detail),\n *     });\n *     setData(await res.json());\n *   }}\n * />\n * ```\n *\n * The form derives field types, validation, and helper labels from the\n * spec. Override per-field via the x-roxy-ui spec extension.\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyEndpointForm as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyEndpointFormProps = ComponentProps<typeof Element>;\n\nexport function RoxyEndpointForm(props: RoxyEndpointFormProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyEndpointForm;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/endpoint-form.tsx"
    }
  ]
}