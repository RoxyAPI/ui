{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-yoga-list",
  "type": "registry:ui",
  "title": "RoxyYogaList",
  "description": "Yoga reference cards from the catalog with optional detail mode",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.25.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-yoga-list.tsx",
      "content": "'use client';\n\n/**\n * <RoxyYogaList data={...} />: Yoga reference cards from the catalog with optional detail mode.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/yoga-list/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.vedicAstrology.listYogas();\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyYogaList as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyYogaListProps = ComponentProps<typeof Element>;\n\nexport function RoxyYogaList(props: RoxyYogaListProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyYogaList;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/yoga-list.tsx"
    }
  ]
}