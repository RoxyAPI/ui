{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-forecast-digest",
  "type": "registry:ui",
  "title": "RoxyForecastDigest",
  "description": "Rolled-up forecast across the next 24 hours, 7, 30, and 90 days with per-domain breakdown and top events",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.12.2/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-forecast-digest.tsx",
      "content": "'use client';\n\n/**\n * <RoxyForecastDigest data={...} />: Rolled-up forecast across the next 24 hours, 7, 30, and 90 days with per-domain breakdown and top events.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/forecast-digest/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyForecastDigest as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyForecastDigestProps = ComponentProps<typeof Element>;\n\nexport function RoxyForecastDigest(props: RoxyForecastDigestProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyForecastDigest;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/forecast-digest.tsx"
    }
  ]
}