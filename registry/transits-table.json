{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-transits-table",
  "type": "registry:ui",
  "title": "RoxyTransitsTable",
  "description": "Live planet positions plus aspects to a natal chart",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-transits-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyTransitsTable data={...} />: Live planet positions plus aspects to a natal chart.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/transits-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.astrology.calculateTransits({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyTransitsTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyTransitsTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyTransitsTable(props: RoxyTransitsTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyTransitsTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/transits-table.tsx"
    }
  ]
}