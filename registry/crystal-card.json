{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-crystal-card",
  "type": "registry:ui",
  "title": "RoxyCrystalCard",
  "description": "Single-crystal detail card with photo, spiritual, emotional, and physical meaning, metaphysical attributes, affirmation, and pairings",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.16.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-crystal-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyCrystalCard data={...} />: Single-crystal detail card with photo, spiritual, emotional, and physical meaning, metaphysical attributes, affirmation, and pairings.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/crystal-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyCrystalCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyCrystalCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyCrystalCard(props: RoxyCrystalCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyCrystalCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/crystal-card.tsx"
    }
  ]
}