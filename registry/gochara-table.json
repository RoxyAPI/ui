{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-gochara-table",
  "type": "registry:ui",
  "title": "RoxyGocharaTable",
  "description": "Where each graha transits now, the natal house it falls in, and the Ashtakavarga-qualified kaksha verdict",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.37.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-gochara-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyGocharaTable data={...} />: Where each graha transits now, the natal house it falls in, and the Ashtakavarga-qualified kaksha verdict.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/gochara-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyGocharaTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyGocharaTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyGocharaTable(props: RoxyGocharaTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyGocharaTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/gochara-table.tsx"
    }
  ]
}