{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-dosha-card",
  "type": "registry:ui",
  "title": "RoxyDoshaCard",
  "description": "Manglik, Kaal Sarp, or Sade Sati presence card",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.31.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-dosha-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyDoshaCard data={...} />: Manglik, Kaal Sarp, or Sade Sati presence card.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/dosha-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.vedicAstrology.checkManglikDosha({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyDoshaCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDoshaCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyDoshaCard(props: RoxyDoshaCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyDoshaCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/dosha-card.tsx"
    }
  ]
}