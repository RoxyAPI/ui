{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-tarot-card",
  "type": "registry:ui",
  "title": "RoxyTarotCard",
  "description": "Single tarot card with upright/reversed flip animation",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.7.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-tarot-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyTarotCard data={...} />: Single tarot card with upright/reversed flip animation.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/tarot-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.tarot.getDailyCard();\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyTarotCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyTarotCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyTarotCard(props: RoxyTarotCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyTarotCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/tarot-card.tsx"
    }
  ]
}