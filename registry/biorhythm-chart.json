{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-biorhythm-chart",
  "type": "registry:ui",
  "title": "RoxyBiorhythmChart",
  "description": "Daily biorhythm bars or multi-day forecast cycle lines",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.22.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-biorhythm-chart.tsx",
      "content": "'use client';\n\n/**\n * <RoxyBiorhythmChart data={...} />: Daily biorhythm bars or multi-day forecast cycle lines.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/biorhythm-chart/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.biorhythm.getDailyBiorhythm({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyBiorhythmChart as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyBiorhythmChartProps = ComponentProps<typeof Element>;\n\nexport function RoxyBiorhythmChart(props: RoxyBiorhythmChartProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyBiorhythmChart;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/biorhythm-chart.tsx"
    }
  ]
}