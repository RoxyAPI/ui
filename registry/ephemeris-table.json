{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-ephemeris-table",
  "type": "registry:ui",
  "title": "RoxyEphemerisTable",
  "description": "Monthly ephemeris: every body day by day, with each sign change and retrograde stretch called out",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.6/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-ephemeris-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyEphemerisTable data={...} />: Monthly ephemeris: every body day by day, with each sign change and retrograde stretch called out.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/ephemeris-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyEphemerisTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyEphemerisTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyEphemerisTable(props: RoxyEphemerisTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyEphemerisTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/ephemeris-table.tsx"
    }
  ]
}