{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-dosha-constitution",
  "type": "registry:ui",
  "title": "RoxyDoshaConstitution",
  "description": "Ayurvedic constitution read from a birth chart: the three humour shares as one bar, the dominant and secondary humour, the three cited factors behind the blend with their verses, the shadbala ranking the third factor reads, and the scope note the response carries",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.40.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-dosha-constitution.tsx",
      "content": "'use client';\n\n/**\n * <RoxyDoshaConstitution data={...} />: Ayurvedic constitution read from a birth chart: the three humour shares as one bar, the dominant and secondary humour, the three cited factors behind the blend with their verses, the shadbala ranking the third factor reads, and the scope note the response carries.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/dosha-constitution/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyDoshaConstitution as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDoshaConstitutionProps = ComponentProps<typeof Element>;\n\nexport function RoxyDoshaConstitution(props: RoxyDoshaConstitutionProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyDoshaConstitution;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/dosha-constitution.tsx"
    }
  ]
}