{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-relocation-wheel",
  "type": "registry:ui",
  "title": "RoxyRelocationWheel",
  "description": "Relocation chart wheel recomputing houses and angles for a new place, with the planets that change house",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.11.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-relocation-wheel.tsx",
      "content": "'use client';\n\n/**\n * <RoxyRelocationWheel data={...} />: Relocation chart wheel recomputing houses and angles for a new place, with the planets that change house.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/relocation-wheel/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyRelocationWheel as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyRelocationWheelProps = ComponentProps<typeof Element>;\n\nexport function RoxyRelocationWheel(props: RoxyRelocationWheelProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyRelocationWheel;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/relocation-wheel.tsx"
    }
  ]
}