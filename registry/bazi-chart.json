{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-bazi-chart",
  "type": "registry:ui",
  "title": "RoxyBaziChart",
  "description": "BaZi four pillars chart: stem over branch per pillar with the Ten God relation, the stems each branch stores, the Na Yin, the Day Master, the five-element balance and every interaction the pillars form",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.37.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-bazi-chart.tsx",
      "content": "'use client';\n\n/**\n * <RoxyBaziChart data={...} />: BaZi four pillars chart: stem over branch per pillar with the Ten God relation, the stems each branch stores, the Na Yin, the Day Master, the five-element balance and every interaction the pillars form.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/bazi-chart/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyBaziChart as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyBaziChartProps = ComponentProps<typeof Element>;\n\nexport function RoxyBaziChart(props: RoxyBaziChartProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyBaziChart;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/bazi-chart.tsx"
    }
  ]
}