{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-hd-connection",
  "type": "registry:ui",
  "title": "RoxyHdConnection",
  "description": "Human Design connection chart: the composite channels two people form, with circuit, dynamic, and which person holds each gate",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.28.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-hd-connection.tsx",
      "content": "'use client';\n\n/**\n * <RoxyHdConnection data={...} />: Human Design connection chart: the composite channels two people form, with circuit, dynamic, and which person holds each gate.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/hd-connection/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyHdConnection as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyHdConnectionProps = ComponentProps<typeof Element>;\n\nexport function RoxyHdConnection(props: RoxyHdConnectionProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyHdConnection;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/hd-connection.tsx"
    }
  ]
}