{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-data",
  "type": "registry:ui",
  "title": "RoxyData",
  "description": "Generic fallback renderer for any OpenAPI response shape",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.1.2/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-data.tsx",
      "content": "'use client';\n\n/**\n * <RoxyData data={...} />: Generic fallback renderer for any OpenAPI response shape.\n *\n * You own this file. Customize freely. The underlying Lit element loads\n * from jsDelivr at runtime via @roxyapi/ui-react, so updates ship without\n * a re-install.\n *\n * Wiring:\n *\n * ```tsx\n * // <RoxyData> is a pure renderer. No fetch, no server route.\n * // Pass any RoxyAPI response directly:\n * <RoxyData data={response} />\n * ```\n *\n * Use this as a fallback for response shapes that do not yet have a\n * dedicated component, or for prototyping a new endpoint.\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyData as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDataProps = ComponentProps<typeof Element>;\n\nexport function RoxyData(props: RoxyDataProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyData;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/data.tsx"
    }
  ]
}