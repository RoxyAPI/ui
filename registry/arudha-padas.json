{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-arudha-padas",
  "type": "registry:ui",
  "title": "RoxyArudhaPadas",
  "description": "The twelve Jaimini arudha padas with their derivation, the Arudha Lagna and the Upapada marked",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.26.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-arudha-padas.tsx",
      "content": "'use client';\n\n/**\n * <RoxyArudhaPadas data={...} />: The twelve Jaimini arudha padas with their derivation, the Arudha Lagna and the Upapada marked.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/arudha-padas/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyArudhaPadas as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyArudhaPadasProps = ComponentProps<typeof Element>;\n\nexport function RoxyArudhaPadas(props: RoxyArudhaPadasProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyArudhaPadas;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/arudha-padas.tsx"
    }
  ]
}