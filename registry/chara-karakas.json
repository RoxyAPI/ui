{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-chara-karakas",
  "type": "registry:ui",
  "title": "RoxyCharaKarakas",
  "description": "Jaimini chara karakas ranked Atmakaraka first, with the scheme that produced the ranking",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.27.4/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-chara-karakas.tsx",
      "content": "'use client';\n\n/**\n * <RoxyCharaKarakas data={...} />: Jaimini chara karakas ranked Atmakaraka first, with the scheme that produced the ranking.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/chara-karakas/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyCharaKarakas as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyCharaKarakasProps = ComponentProps<typeof Element>;\n\nexport function RoxyCharaKarakas(props: RoxyCharaKarakasProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyCharaKarakas;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/chara-karakas.tsx"
    }
  ]
}