{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-positions-table",
  "type": "registry:ui",
  "title": "RoxyPositionsTable",
  "description": "Editorial positions table for asteroids, Black Moon Lilith, secondary progressions, solar arc directions, and the Arabic lots",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.15.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-positions-table.tsx",
      "content": "'use client';\n\n/**\n * <RoxyPositionsTable data={...} />: Editorial positions table for asteroids, Black Moon Lilith, secondary progressions, solar arc directions, and the Arabic lots.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/positions-table/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyPositionsTable as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyPositionsTableProps = ComponentProps<typeof Element>;\n\nexport function RoxyPositionsTable(props: RoxyPositionsTableProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyPositionsTable;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/positions-table.tsx"
    }
  ]
}