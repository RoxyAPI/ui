{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-natal-chart",
  "type": "registry:ui",
  "title": "RoxyNatalChart",
  "description": "Western natal chart wheel for /astrology/natal-chart responses",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.10.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-natal-chart.tsx",
      "content": "'use client';\n\n/**\n * <RoxyNatalChart data={...} />: Western natal chart wheel for /astrology/natal-chart responses.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/natal-chart/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.astrology.generateNatalChart({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyNatalChart as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyNatalChartProps = ComponentProps<typeof Element>;\n\nexport function RoxyNatalChart(props: RoxyNatalChartProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyNatalChart;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/natal-chart.tsx"
    }
  ]
}