{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-vastu-mandala",
  "type": "registry:ui",
  "title": "RoxyVastuMandala",
  "description": "Vastu Purusha Mandala projected over a plot: every pada with the devata holding it, the brahmasthan block, the vamsa diagonals and where they cross, and in the entrance read the square the main door falls on with the verse effect for that pada",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.40.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-vastu-mandala.tsx",
      "content": "'use client';\n\n/**\n * <RoxyVastuMandala data={...} />: Vastu Purusha Mandala projected over a plot: every pada with the devata holding it, the brahmasthan block, the vamsa diagonals and where they cross, and in the entrance read the square the main door falls on with the verse effect for that pada.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/vastu-mandala/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyVastuMandala as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyVastuMandalaProps = ComponentProps<typeof Element>;\n\nexport function RoxyVastuMandala(props: RoxyVastuMandalaProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyVastuMandala;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/vastu-mandala.tsx"
    }
  ]
}