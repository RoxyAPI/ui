{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-hd-type-card",
  "type": "registry:ui",
  "title": "RoxyHdTypeCard",
  "description": "Human Design type reading without the chart: type, strategy, authority, and profile, with the aura mechanic, the signature and not-self themes, and the interpretation behind each label. Also renders the profile response with its two line keynotes",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.18.5/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-hd-type-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyHdTypeCard data={...} />: Human Design type reading without the chart: type, strategy, authority, and profile, with the aura mechanic, the signature and not-self themes, and the interpretation behind each label. Also renders the profile response with its two line keynotes.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/hd-type-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyHdTypeCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyHdTypeCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyHdTypeCard(props: RoxyHdTypeCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyHdTypeCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/hd-type-card.tsx"
    }
  ]
}