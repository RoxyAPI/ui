{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "roxy-dream-card",
  "type": "registry:ui",
  "title": "RoxyDreamCard",
  "description": "Dream symbol card with the symbol name, full interpretation, and dictionary letter",
  "dependencies": [
    "@roxyapi/ui-react"
  ],
  "registryDependencies": [
    "https://cdn.jsdelivr.net/gh/RoxyAPI/ui@v0.40.0/registry/theme.json"
  ],
  "files": [
    {
      "path": "components/roxy-dream-card.tsx",
      "content": "'use client';\n\n/**\n * <RoxyDreamCard data={...} />: Dream symbol card with the symbol name, full interpretation, and dictionary letter.\n *\n * You own this file. Customize freely. The component is re-exported from\n * @roxyapi/ui-react and stays in sync with the published package.\n *\n * Typical wiring (server-side fetch, client-side render):\n *\n * ```ts\n * // app/api/dream-card/route.ts\n * import { createRoxy } from '@roxyapi/sdk';\n * const roxy = createRoxy(process.env.ROXY_API_KEY!);\n * export async function POST(req: Request) {\n *   const { data } = await roxy.someDomain.someMethod({ body: await req.json() });\n *   return Response.json(data);\n * }\n * ```\n *\n * Theme via --roxy-* CSS custom properties on :root (see globals.css).\n */\nimport { RoxyDreamCard as Element } from '@roxyapi/ui-react';\nimport type { ComponentProps } from 'react';\n\nexport type RoxyDreamCardProps = ComponentProps<typeof Element>;\n\nexport function RoxyDreamCard(props: RoxyDreamCardProps) {\n\treturn <Element {...props} />;\n}\n\nexport default RoxyDreamCard;\n",
      "type": "registry:ui",
      "target": "~/components/roxy-ui/dream-card.tsx"
    }
  ]
}